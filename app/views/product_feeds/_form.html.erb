<%= form_with(model: product_feed, local: true) do |form| %>
  <% if product_feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_feed.errors.count, "error") %> prohibited this product_feed from being saved:</h2>

      <ul>
      <% product_feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
